{
	"name": "@examples/todos",
	"version": "0.1.0",
	"private": true,
	"license": "Unlicense",
	"type": "module",
	"scripts": {
		"build": "parcel build src/index.html --dist-dir dist --public-url /",
		"check": "npm run typecheck && npm run test:e2e",
		"preview": "tsx server.ts",
		"start": "npm run build && npm run preview",
		"test:e2e": "npm run build && playwright test",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@charliewilco/modern-backbone": "0.1.0",
		"@charliewilco/modern-handlebars": "0.1.0"
	},
	"devDependencies": {
		"@playwright/test": "^1.62.1",
		"@types/node": "^24.13.3",
		"parcel": "^2.16.4",
		"tsx": "^4.23.13",
		"typescript": "^7.0.2"
	}
}
